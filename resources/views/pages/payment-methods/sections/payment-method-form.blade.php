@php($editing = isset($paymentMethod))
<div class="form-grid">
    <div class="form-group"><label class="form-label" for="code">Kode <span aria-hidden="true">*</span></label><input class="form-control @error('code') is-invalid @enderror" id="code" name="code" type="text" maxlength="30" required value="{{ old('code', $paymentMethod->code ?? '') }}" autocomplete="off" placeholder="Contoh: DEBIT_CARD">@error('code')<span class="form-error">{{ $message }}</span>@enderror</div>
    <div class="form-group"><label class="form-label" for="name">Nama <span aria-hidden="true">*</span></label><input class="form-control @error('name') is-invalid @enderror" id="name" name="name" type="text" maxlength="255" required value="{{ old('name', $paymentMethod->name ?? '') }}">@error('name')<span class="form-error">{{ $message }}</span>@enderror</div>
    <div class="form-group"><label class="form-label" for="type">Jenis <span aria-hidden="true">*</span></label><select class="form-control @error('type') is-invalid @enderror" id="type" name="type" required><option value="">Pilih jenis</option><option value="cash" @selected(old('type', $paymentMethod->type ?? '') === 'cash')>Tunai</option><option value="non_cash" @selected(old('type', $paymentMethod->type ?? '') === 'non_cash')>Non-Tunai</option><option value="other" @selected(old('type', $paymentMethod->type ?? '') === 'other')>Lainnya</option></select>@error('type')<span class="form-error">{{ $message }}</span>@enderror</div>
    <div class="form-group"><label class="form-label" for="sort_order">Urutan Tampil <span aria-hidden="true">*</span></label><input class="form-control @error('sort_order') is-invalid @enderror" id="sort_order" name="sort_order" type="number" min="0" max="9999" required value="{{ old('sort_order', $paymentMethod->sort_order ?? 0) }}">@error('sort_order')<span class="form-error">{{ $message }}</span>@enderror</div>
</div>
<div class="form-actions"><a class="btn btn-secondary" href="{{ $editing ? route('payment-methods.show', $paymentMethod) : route('payment-methods.index') }}">Batal</a><button class="btn btn-primary" type="submit">{{ $editing ? 'Simpan Perubahan' : 'Tambah Metode' }}</button></div>
