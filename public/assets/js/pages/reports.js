const page=document.querySelector('[data-report-page]');if(page){const form=page.querySelector('[data-report-filter]');const period=form?.querySelector('[data-report-period]');const custom=form?.querySelector('[data-report-custom]');const update=()=>{if(!period||!custom)return;const active=period.value==='custom';custom.hidden=!active;form.elements.date_from.required=active;form.elements.date_to.required=active};period?.addEventListener('change',update);update();form?.addEventListener('submit',()=>{const button=form.querySelector('[data-report-submit]');if(button){button.disabled=true;button.textContent='Memuat…'}})}
